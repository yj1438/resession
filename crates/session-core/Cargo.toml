[package]
name = "session-core"
version = "0.1.0"
edition = "2024"
description = "Agent-agnostic session discovery, parsing and resume-command building for ReSession"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
